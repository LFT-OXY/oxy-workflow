# 无状态安装器：探测即状态，不维护 Manifest

doctor、向导复装提示、卸载所需的"装没装"状态，全部来自对目录
条目结构化 check 的实时探测，不落任何状态文件。上一轮会话曾把
Manifest（安装清单）定为核心概念，本决策将其废除：状态文件在
用户绕过安装器手动改环境后必然失真，而实时探测天然幂等、永不
说谎。代价是无法区分组件归属（经本安装器装的还是用户自己装的），
由卸载时逐项确认弥补；卸载只操作探测可见且机制可逆的组件。

**Status**: accepted
