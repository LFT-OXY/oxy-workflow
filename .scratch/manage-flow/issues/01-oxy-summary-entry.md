# 01 oxy-summary 入目录

Status: ready-for-agent

推送 `~/.agents/skills/oxy-summary` 内容到 public 集合仓库
`LFT-OXY/skills`（结构 `skills/oxy-summary/`，附极简 README，剔除
.DS_Store），验证 tree API 与 raw 抓取后，在 `entries.ts` 新增
条目（PRD 决议 1-6）。目录条目必须在仓库可抓取之后落地，否则
安装 404。

## Comments

- 2026-07-13 已交付：LFT-OXY/skills 已推送并验证（tree API +
  raw 抓取），条目落地 entries.ts（目录不变量 6/6），沙箱 e2e
  真实抓取安装 + 卸载往返通过。
